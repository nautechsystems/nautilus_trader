version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - yum update -y
      - curl -O https://bootstrap.pypa.io/get-pip.py
      - python3 get-pip.py --user
      - python3 --version
      - python3 -m pip --version
  pre_build:
    commands:
      - yum install -y redis
      - redis-server --daemonize yes
  build:
    commands:
      - echo Build started on `date`
      - python3 -m pip install -r requirements.txt
      - python3 setup.py build_ext --inplace
  post_build:
    commands:
      - cd scripts
      - python3 cleanup.py
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
  name: nautilus-trader-$CODEBUILD_BUILD_NUMBER
  discard-paths: no