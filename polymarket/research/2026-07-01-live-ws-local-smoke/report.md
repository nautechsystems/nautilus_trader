# Optional local live WS smoke

This experiment is optional and requires `POLYMARKET_LIVE_WS_NDJSON` to point at a local full raw WebSocket NDJSON capture. Large raw captures must not be committed to this repository.
