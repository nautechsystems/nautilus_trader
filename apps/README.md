# Apps

Operator-facing applications live here.

These apps serve the `flux` product and deploy surface. They consume engine and system APIs, but they do not rename the engine runtime namespace away from `nautilus_trader`.
