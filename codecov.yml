codecov:
  branch: master
  notify:
    after_n_builds: 10
comment: false

coverage:
  precision: 2
  round: down
  range: "60...95"
  status:
    project:
      default:
        target: "90"
    patch:
      default:
        target: "50"

github_checks:
  annotations: false
