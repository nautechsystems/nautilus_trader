// AUTO-GENERATED by scripts/generate-enums.ts — DO NOT EDIT
//
// Source: nautilus_trader/core/includes/model.h (cbindgen)

/** The type of order book action for an order book event. */
export const BookAction = {
  /** An order is added to the book. */
  ADD: 1,
  /** An existing order in the book is updated/modified. */
  UPDATE: 2,
  /** An existing order in the book is deleted/canceled. */
  DELETE: 3,
  /** The state of the order book is cleared. */
  CLEAR: 4,
} as const;
export type BookAction = (typeof BookAction)[keyof typeof BookAction];

/** The order side for a specific order, or action related to orders. */
export const OrderSide = {
  /** No order side is specified. */
  NO_ORDER_SIDE: 0,
  /** The order is a BUY. */
  BUY: 1,
  /** The order is a SELL. */
  SELL: 2,
} as const;
export type OrderSide = (typeof OrderSide)[keyof typeof OrderSide];

/** The side for the aggressing order of a trade in a market. */
export const AggressorSide = {
  /** There was no specific aggressor for the trade. */
  NO_AGGRESSOR: 0,
  /** The BUY order was the aggressor for the trade. */
  BUYER: 1,
  /** The SELL order was the aggressor for the trade. */
  SELLER: 2,
} as const;
export type AggressorSide = (typeof AggressorSide)[keyof typeof AggressorSide];

/** The type of price for an instrument in a market. */
export const PriceType = {
  /** The best quoted price at which buyers are willing to buy. */
  BID: 1,
  /** The best quoted price at which sellers are willing to sell. */
  ASK: 2,
  /** The arithmetic midpoint between the best bid and ask quotes. */
  MID: 3,
  /** The price at which the last trade of an instrument was executed. */
  LAST: 4,
  /** A reference price reflecting an instrument's fair value. */
  MARK: 5,
} as const;
export type PriceType = (typeof PriceType)[keyof typeof PriceType];

/** An aggregation source for derived data. */
export const AggregationSource = {
  /** The data is externally aggregated (outside the Nautilus system boundary). */
  EXTERNAL: 1,
  /** The data is internally aggregated (inside the Nautilus system boundary). */
  INTERNAL: 2,
} as const;
export type AggregationSource = (typeof AggregationSource)[keyof typeof AggregationSource];

/** The type of event for an instrument close. */
export const InstrumentCloseType = {
  /** When the market session ended. */
  END_OF_SESSION: 1,
  /** When the instrument expiration was reached. */
  CONTRACT_EXPIRED: 2,
} as const;
export type InstrumentCloseType = (typeof InstrumentCloseType)[keyof typeof InstrumentCloseType];

/** An account type provided by a trading venue or broker. */
export const AccountType = {
  /** An account with unleveraged cash assets only. */
  CASH: 1,
  /** An account which facilitates trading on margin. */
  MARGIN: 2,
  /** An account specific to betting markets. */
  BETTING: 3,
  /** An account which represents a blockchain wallet. */
  WALLET: 4,
} as const;
export type AccountType = (typeof AccountType)[keyof typeof AccountType];

/** A broad financial market asset class. */
export const AssetClass = {
  /** Foreign exchange (FOREX) assets. */
  FX: 1,
  /** Equity / stock assets. */
  EQUITY: 2,
  /** Commodity assets. */
  COMMODITY: 3,
  /** Debt based assets. */
  DEBT: 4,
  /** Index based assets (baskets). */
  INDEX: 5,
  /** Cryptocurrency or crypto token assets. */
  CRYPTOCURRENCY: 6,
  /** Alternative assets. */
  ALTERNATIVE: 7,
} as const;
export type AssetClass = (typeof AssetClass)[keyof typeof AssetClass];

/** The instrument class. */
export const InstrumentClass = {
  /** A spot market instrument class. */
  SPOT: 1,
  /** A swap instrument class. */
  SWAP: 2,
  /** A futures contract instrument class. */
  FUTURE: 3,
  /** A futures spread instrument class. */
  FUTURES_SPREAD: 4,
  /** A forward derivative instrument class. */
  FORWARD: 5,
  /** A contract-for-difference (CFD) instrument class. */
  CFD: 6,
  /** A bond instrument class. */
  BOND: 7,
  /** An option contract instrument class. */
  OPTION: 8,
  /** An option spread instrument class. */
  OPTION_SPREAD: 9,
  /** A warrant instrument class. */
  WARRANT: 10,
  /** A sports betting instrument class. */
  SPORTS_BETTING: 11,
  /** A binary option instrument class. */
  BINARY_OPTION: 12,
} as const;
export type InstrumentClass = (typeof InstrumentClass)[keyof typeof InstrumentClass];

/** The order book type. */
export const BookType = {
  /** Top-of-book best bid/ask, one level per side. */
  L1_MBP: 1,
  /** Market by price, one order per level (aggregated). */
  L2_MBP: 2,
  /** Market by order, multiple orders per level (full granularity). */
  L3_MBO: 3,
} as const;
export type BookType = (typeof BookType)[keyof typeof BookType];

/** The order contingency type which specifies the behavior of linked orders. */
export const ContingencyType = {
  /** Not a contingent order. */
  NO_CONTINGENCY: 0,
  /** One-Cancels-the-Other. */
  OCO: 1,
  /** One-Triggers-the-Other. */
  OTO: 2,
  /** One-Updates-the-Other (by proportional quantity). */
  OUO: 3,
} as const;
export type ContingencyType = (typeof ContingencyType)[keyof typeof ContingencyType];

/** The broad currency type. */
export const CurrencyType = {
  /** A type of cryptocurrency or crypto token. */
  CRYPTO: 1,
  /** A type of currency issued by governments which is not backed by a commodity. */
  FIAT: 2,
  /** A type of currency that is based on the value of an underlying commodity. */
  COMMODITY_BACKED: 3,
} as const;
export type CurrencyType = (typeof CurrencyType)[keyof typeof CurrencyType];

/** The liquidity side for a trade. */
export const LiquiditySide = {
  /** No liquidity side specified. */
  NO_LIQUIDITY_SIDE: 0,
  /** The order passively provided liquidity to the market. */
  MAKER: 1,
  /** The order aggressively took liquidity from the market. */
  TAKER: 2,
} as const;
export type LiquiditySide = (typeof LiquiditySide)[keyof typeof LiquiditySide];

/** The status of an individual market on a trading venue. */
export const MarketStatus = {
  /** The instrument is trading. */
  OPEN: 1,
  /** The instrument is in a pre-open period. */
  CLOSED: 2,
  /** Trading in the instrument has been paused. */
  PAUSED: 3,
  /** Trading in the instrument has been suspended. */
  SUSPENDED: 5,
  /** Trading in the instrument is not available. */
  NOT_AVAILABLE: 6,
} as const;
export type MarketStatus = (typeof MarketStatus)[keyof typeof MarketStatus];

/** An action affecting the status of an individual market on a trading venue. */
export const MarketStatusAction = {
  /** No change. */
  NONE: 0,
  /** The instrument is in a pre-open period. */
  PRE_OPEN: 1,
  /** The instrument is in a pre-cross period. */
  PRE_CROSS: 2,
  /** The instrument is quoting but not trading. */
  QUOTING: 3,
  /** The instrument is in a cross/auction. */
  CROSS: 4,
  /** The instrument is being opened through a trading rotation. */
  ROTATION: 5,
  /** A new price indication is available for the instrument. */
  NEW_PRICE_INDICATION: 6,
  /** The instrument is trading. */
  TRADING: 7,
  /** Trading in the instrument has been halted. */
  HALT: 8,
  /** Trading in the instrument has been paused. */
  PAUSE: 9,
  /** Trading in the instrument has been suspended. */
  SUSPEND: 10,
  /** The instrument is in a pre-close period. */
  PRE_CLOSE: 11,
  /** Trading in the instrument has closed. */
  CLOSE: 12,
  /** The instrument is in a post-close period. */
  POST_CLOSE: 13,
  /** A change in short-selling restrictions. */
  SHORT_SELL_RESTRICTION_CHANGE: 14,
  /** The instrument is not available for trading. */
  NOT_AVAILABLE_FOR_TRADING: 15,
} as const;
export type MarketStatusAction = (typeof MarketStatusAction)[keyof typeof MarketStatusAction];

/** The order management system (OMS) type. */
export const OmsType = {
  /** No specific type of order management specified. */
  UNSPECIFIED: 0,
  /** The netting type where there is one position per instrument. */
  NETTING: 1,
  /** The hedging type where there can be multiple positions per instrument. */
  HEDGING: 2,
} as const;
export type OmsType = (typeof OmsType)[keyof typeof OmsType];

/** The kind of option contract. */
export const OptionKind = {
  /** A Call option. */
  CALL: 1,
  /** A Put option. */
  PUT: 2,
} as const;
export type OptionKind = (typeof OptionKind)[keyof typeof OptionKind];

/** Defines when OTO (One-Triggers-Other) child orders are released. */
export const OtoTriggerMode = {
  /** Release child order(s) pro-rata to each partial fill (default). */
  PARTIAL: 0,
  /** Release child order(s) only once the parent is fully filled. */
  FULL: 1,
} as const;
export type OtoTriggerMode = (typeof OtoTriggerMode)[keyof typeof OtoTriggerMode];

/** The status for a specific order. */
export const OrderStatus = {
  /** The order is initialized (instantiated) within the Nautilus system. */
  INITIALIZED: 1,
  /** The order was denied by the Nautilus system. */
  DENIED: 2,
  /** The order became emulated by the Nautilus system. */
  EMULATED: 3,
  /** The order was released from the OrderEmulator component. */
  RELEASED: 4,
  /** The order was submitted to the external service or trading venue. */
  SUBMITTED: 5,
  /** The order was acknowledged by the trading venue. */
  ACCEPTED: 6,
  /** The order was rejected by the trading venue. */
  REJECTED: 7,
  /** The order was canceled (closed/done). */
  CANCELED: 8,
  /** The order reached a GTD expiration (closed/done). */
  EXPIRED: 9,
  /** The order STOP price was triggered on a trading venue. */
  TRIGGERED: 10,
  /** The order is currently pending a request to modify. */
  PENDING_UPDATE: 11,
  /** The order is currently pending a request to cancel. */
  PENDING_CANCEL: 12,
  /** The order has been partially filled. */
  PARTIALLY_FILLED: 13,
  /** The order has been completely filled (closed/done). */
  FILLED: 14,
} as const;
export type OrderStatus = (typeof OrderStatus)[keyof typeof OrderStatus];

/** The type of order. */
export const OrderType = {
  /** A market order. */
  MARKET: 1,
  /** A limit order. */
  LIMIT: 2,
  /** A stop market order. */
  STOP_MARKET: 3,
  /** A stop limit order. */
  STOP_LIMIT: 4,
  /** A market-to-limit order. */
  MARKET_TO_LIMIT: 5,
  /** A market-if-touched order. */
  MARKET_IF_TOUCHED: 6,
  /** A limit-if-touched order. */
  LIMIT_IF_TOUCHED: 7,
  /** A trailing stop market order. */
  TRAILING_STOP_MARKET: 8,
  /** A trailing stop limit order. */
  TRAILING_STOP_LIMIT: 9,
} as const;
export type OrderType = (typeof OrderType)[keyof typeof OrderType];

/** The market side for a specific position, or action related to positions. */
export const PositionSide = {
  /** No position side is specified. */
  NO_POSITION_SIDE: 0,
  /** A neutral/flat position. */
  FLAT: 1,
  /** A long position in the market. */
  LONG: 2,
  /** A short position in the market. */
  SHORT: 3,
} as const;
export type PositionSide = (typeof PositionSide)[keyof typeof PositionSide];

/** The type of position adjustment. */
export const PositionAdjustmentType = {
  /** Commission adjustment affecting position quantity. */
  COMMISSION: 1,
  /** Funding payment affecting position realized PnL. */
  FUNDING: 2,
} as const;
export type PositionAdjustmentType = (typeof PositionAdjustmentType)[keyof typeof PositionAdjustmentType];

/** A record flag bit field, indicating event end and data information. */
export const RecordFlag = {
  /** Last message in the book event or packet. */
  F_LAST: 128,
  /** Top-of-book message, not an individual order. */
  F_TOB: 64,
  /** Message sourced from a replay, such as a snapshot server. */
  F_SNAPSHOT: 32,
  /** Aggregated price level message, not an individual order. */
  F_MBP: 16,
  /** Reserved for future use. */
  RESERVED_2: 8,
  /** Reserved for future use. */
  RESERVED_1: 4,
} as const;
export type RecordFlag = (typeof RecordFlag)[keyof typeof RecordFlag];

/** The 'Time in Force' instruction for an order. */
export const TimeInForce = {
  /** Good Till Cancel (GTC). */
  GTC: 1,
  /** Immediate or Cancel (IOC). */
  IOC: 2,
  /** Fill or Kill (FOK). */
  FOK: 3,
  /** Good Till Date (GTD). */
  GTD: 4,
  /** Day order. */
  DAY: 5,
  /** At the Opening (ATO). */
  AT_THE_OPEN: 6,
  /** At the Closing (ATC). */
  AT_THE_CLOSE: 7,
} as const;
export type TimeInForce = (typeof TimeInForce)[keyof typeof TimeInForce];

/** The trading state for a node. */
export const TradingState = {
  /** Normal trading operations. */
  ACTIVE: 1,
  /** Trading is completely halted. */
  HALTED: 2,
  /** Only order commands which would cancel or reduce positions. */
  REDUCING: 3,
} as const;
export type TradingState = (typeof TradingState)[keyof typeof TradingState];

/** The trailing offset type for an order type. */
export const TrailingOffsetType = {
  /** No trailing offset type is specified. */
  NO_TRAILING_OFFSET: 0,
  /** The trailing offset is based on a market price. */
  PRICE: 1,
  /** The trailing offset is based on a percentage in basis points. */
  BASIS_POINTS: 2,
  /** The trailing offset is based on the number of ticks. */
  TICKS: 3,
  /** The trailing offset is based on a price tier. */
  PRICE_TIER: 4,
} as const;
export type TrailingOffsetType = (typeof TrailingOffsetType)[keyof typeof TrailingOffsetType];

/** The trigger type for the stop/trigger price of an order. */
export const TriggerType = {
  /** No trigger type is specified. */
  NO_TRIGGER: 0,
  /** The default trigger type set by the trading venue. */
  DEFAULT: 1,
  /** Based on the last traded price for the instrument. */
  LAST_PRICE: 2,
  /** Based on the mark price for the instrument. */
  MARK_PRICE: 3,
  /** Based on the index price for the instrument. */
  INDEX_PRICE: 4,
  /** Based on the top-of-book quoted prices for the instrument. */
  BID_ASK: 5,
  /** Based on a 'double match' of the last traded price. */
  DOUBLE_LAST: 6,
  /** Based on a 'double match' of the bid/ask price. */
  DOUBLE_BID_ASK: 7,
  /** Based on both the LastPrice and BidAsk. */
  LAST_OR_BID_ASK: 8,
  /** Based on the mid-point of the BidAsk. */
  MID_POINT: 9,
} as const;
export type TriggerType = (typeof TriggerType)[keyof typeof TriggerType];
