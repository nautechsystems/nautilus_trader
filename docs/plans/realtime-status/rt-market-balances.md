# Realtime Lane Status

- lane: `lanes/task-15b-rt-balances-standard`
- owner: `coordinator`
- branch: `lanes/task-15b-rt-balances-standard`
- worktree: `.worktrees/task-15b-rt-balances-standard`
- depends_on: `Task 8: Refactor Shared Legacy Socket Adapter For Remaining Surfaces`
- write_scope: `fluxboard/MarketData.tsx`, `fluxboard/Balances.tsx`, `fluxboard/MarketData.test.tsx`, `fluxboard/Balances.test.tsx`, `fluxboard/__tests__/realtime/market-balances-standard.test.tsx`, `fluxboard/__tests__/panels/market-balances.perf.test.tsx`, `fluxboard/e2e/realtime-cutovers/market-balances.spec.ts`, `docs/plans/realtime-surfaces/marketdata-cutover.md`, `docs/plans/realtime-surfaces/balances-cutover.md`
- rollout_control: `marketdata + balances flags + backend capability`
- rollback_trigger: `freshness timer regression or mixed-contract drift`
- current status: `completed`
- active task: `Task 15b: Harden Balances Standard Contract And Cleanup Evidence`
- current commit or diff: `working tree follow-up on lanes/task-15b-rt-balances-standard`
- cutover_packet: `docs/plans/realtime-surfaces/marketdata-cutover.md ; docs/plans/realtime-surfaces/balances-cutover.md`
- canary_scope: `balances realtime surface; marketData historical evidence only after parking`
- minimum_canary_cohort: `Balances: 1 internal profile-scoped /balances canary for 7 consecutive days; MarketData: non-blocking while parked from the active cleanup wave`
- minimum_standard_subscribers: `Balances: 1 flagged Balances standard subscriber during the cleanup review window; MarketData: non-blocking while parked`
- minimum_standard_event_volume: `Balances: backend proof of at least one flagged balances standard subscriber on 3 distinct canary days plus at least one balances invalidate event during the cleanup review window; heartbeat counts are supporting liveness evidence only. MarketData remains non-blocking historical evidence while parked`
- alert_state: `green in lane-owned verification with compatibility-bridge limitations recorded in the cutover packets`
- rollback_exercise_result: `pass via task-owned flag-off remount exercise in __tests__/realtime/market-balances-standard.test.tsx; live mid-session flag flips still require remount`
- dashboards_playbooks: `systems/flux/docs/realtime-rollout.md#Observability ; systems/flux/docs/realtime-rollout.md#Operational-guidance`
- rollout_metrics_snapshot: `Balances cleanup review must anchor on backend active_standard_subscribers.balances:v2, standard_subscribe_counts, standard_event_counts.balances:heartbeat / balances:invalidate, and task-owned request-count plus freshness evidence; MarketData remains parked and continues to rely on historical bridge metrics only`
- legacy_traffic_status: `legacy-default`
- verification run: `Task 15b follow-up green in the balances lane: PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /home/ubuntu/nautilus-trader-dev/.worktrees/task-3-rt-backend/.venv/bin/pytest -q tests/unit_tests/flux/api/test_realtime_contract.py -k balances --confcutdir=tests/unit_tests/flux/api passed (10 tests), including default-route lineage fallback, canonical-source invalidation regressions, the first-tick post-subscribe balances invalidation race, and the legacy market_update rollback-path regression for balances-only changes. Task 15b follow-up green: VITEST_FULL=1 ../../rt-controller/fluxboard/node_modules/.bin/vitest run Balances.test.tsx api.flux.test.ts __tests__/realtime/market-balances-standard.test.tsx passed (75 tests), including backend-shaped lineage fixtures and invalidate-refresh lineage-stability coverage. Task 15b follow-up green: pnpm build:test passed. Task 15b follow-up green: E2E_BASE_URL=http://127.0.0.1:4173 pnpm exec playwright test -c playwright.smoke.config.ts e2e/realtime-cutovers/market-balances.spec.ts passed (2 tests). Existing shared MarketData coverage still emits React act() warnings in the combined Vitest slice.`
- blockers: `Balances follow-up must preserve canonical portfolio-snapshot invalidation coverage while MarketData stays parked and non-blocking for the active cleanup wave`
- notes_last_update: `Controller completed the original Task 10 TDD lane after reclaiming ownership from the failed wrong-workspace subagent passes. Task 10 delivered the owned standard wiring tests, polling-fallback perf tests, the cutover e2e spec, and production-grade cutover packets. After the first realtime-risk review surfaced burst-handling flaws, the lane added serialized invalidate-only refreshes for both MarketData and Balances plus a task-owned flag-off remount exercise. After the follow-up spec review surfaced the freshness-clock gap, the lane moved both surfaces onto shared viewport clocks, removed task-local freshness intervals, added explicit MarketData-only and Balances-only rollout coverage, and tightened MarketData's proof to mounted-page behavior with bounded freshness fanout, stable page anchors, and no extra memoized sort before paging. Task 15b then superseded the original Balances bridge-backed assumptions by moving Balances onto backend-issued `contract_version=2` lineage and standard Socket.IO subscription semantics while retaining MarketData parking. Balances steady-state live traffic now uses standard Socket.IO `subscribe` / `realtime_event` / `unsubscribe`, while the advertised `transport_mode=polling_only` and `replay_supported=false` capabilities still describe the recovery path because canonical refresh remains snapshot based. The latest follow-up also aligned default-route lineage with the backend default descriptor, removed invalidate-refresh resubscribe churn while stream identity stays stable, switched degraded balances invalidation to canonical snapshot semantics, reduced backend balances signature work to active balances-subscriber ticks only, seeded the balances baseline at first standard subscribe so the first post-subscribe canonical change emits `invalidate` instead of a false `heartbeat`, and restored legacy `market_update` invalidation on balances-only changes so the explicit flag-off rollback path stays live while MarketData remains parked.`
- next handoff: `Balances no longer needs a bridge-backed cleanup exception once the Task 15b follow-up is integrated. MarketData remains parked from the active cleanup wave for now; no further cleanup-wave implementation is planned for it unless product later chooses explicit retirement or funds a dedicated backend contract.`
