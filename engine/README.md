# Engine

Canonical home for the reusable trading engine.

Runtime identity remains `nautilus_trader` during this migration. The `engine/` directory is the repo-level ownership boundary for core runtime code, even while the surrounding product and deploy surface is referred to as `flux`.
