codecov:
  branch: master

coverage:
  precision: 2
  round: down
  range: "60...100"
  status:
    project:
      default:
        target: "89"  # Temporary
    patch:
      default:
        target: "50"
