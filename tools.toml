# NautilusTrader-specific version pins for tools without a native manifest.
#
# Shared tools:      .nautilus-engineering/tools.toml
# Shared source:     https://github.com/nautechsystems/nautilus_engineering
# Rust toolchain:    rust-toolchain.toml
# Nightly toolchain: tools.toml [miri]
# Local Cargo CLIs:  Cargo.toml [workspace.metadata.tools]
# Local CLIs:        tools.toml
# uv range:          python/pyproject.toml [tool.uv]

[miri]
version = "nightly-2026-08-23"
rustc-version = "1.100.0"

[pypi-attestations]
version = "0.0.30"
