# Flux

Flux is a standalone system built on top of the engine.

## Canonical locations

- Python package: `systems/flux/flux/`
- Docs: `systems/flux/docs/`
- Canonical imports: `flux.*`
- Compatibility imports: `flux.*`
