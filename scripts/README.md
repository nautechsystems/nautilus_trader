# Legacy script compatibility paths

Canonical script ownership has moved to:

- `tooling/dev/`
- `tooling/ci/`
- `tooling/release/`
- `ops/scripts/deploy/`

The `scripts/` tree is kept as a compatibility layer via symlinks while callers migrate to the canonical paths.
