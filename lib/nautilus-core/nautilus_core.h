#ifndef add_h
#define add_h

/* Warning, this file is autogenerated by cbindgen. Don't modify this manually. */

#include <stdint.h>

/**
 * Represents a datetime
 */
typedef struct DateTime {
  uint32_t year;
  uint32_t month;
  uint32_t day;
  uint32_t hour;
  uint32_t minute;
  uint32_t second;
  uint32_t microsecond;
} DateTime;

/**
 * Return the current date time in UTC.
 */
struct DateTime c_utc_now(void);

/**
 * Return the current seconds since the Unix epoch.
 */
double c_timestamp(void);

/**
 * Return the current milliseconds since the Unix epoch.
 */
uint64_t c_timestamp_ms(void);

/**
 * Return the current microseconds since the Unix epoch.
 */
uint64_t c_timestamp_us(void);

char *c_uuid_str_new(void);

void c_uuid_str_free(char *s);

#endif /* add_h */
