name: build

# Build and publish the Python package, Rust crates, and stable release assets

permissions:
  # Principle of least privilege
  contents: read
  actions: read

on:
  push:
    branches:
      - test-ci
      - test-pre-commit
      - develop
      - nightly
      - master
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - edited
    branches:
      - develop

# Isolate metadata-only edits; push groups include the branch and commit
concurrency:
  group: >-
    ${{ github.event_name == 'pull_request'
    && github.event.action == 'edited'
    && !github.event.changes.base
    && format('{0}-metadata-{1}', github.workflow, github.run_id)
    || format('{0}-{1}', github.workflow,
    github.event.pull_request.number || format('{0}-{1}', github.ref_name, github.sha)) }}
  cancel-in-progress: >-
    ${{ github.event_name == 'pull_request'
    && !(github.event.action == 'edited' && !github.event.changes.base) }}

env:
  AUDIT_PR: >-
    ${{ github.event_name == 'pull_request'
    && (!github.event.pull_request.head.repo.full_name
    || github.event.pull_request.head.repo.full_name != github.repository) }}
  UNTRUSTED_PR: >-
    ${{ github.event_name == 'pull_request'
    && (!github.event.pull_request.head.repo.full_name
    || github.event.pull_request.head.repo.full_name != github.repository
    || !github.event.pull_request.user.login
    || github.event.pull_request.user.login == 'dependabot[bot]') }}
  PACKAGE_DIR: python

jobs:
  plan:
    name: plan
    if: >-
      github.event_name != 'pull_request' ||
      github.event.action != 'edited' ||
      github.event.changes.base
    runs-on: ubuntu-latest
    outputs:
      run-tests: ${{ steps.plan.outputs.run_tests }}
      run-rust-tests: ${{ steps.plan.outputs.run_rust_tests }}
      security-audit-needed: ${{ steps.security-audit.outputs.audit_needed }}
      untrusted_pr: ${{ steps.policy.outputs.untrusted_pr }}
      publish_r2: ${{ steps.wheel-publication.outputs.publish_r2 }}
      publish_development: ${{ steps.wheel-publication.outputs.publish_development }}
      publish_environment: ${{ steps.wheel-publication.outputs.publish_environment }}
      wheel_matrix: ${{ steps.wheel-publication.outputs.wheel_matrix }}
      wheel_version: ${{ steps.wheel-publication.outputs.wheel_version }}
    steps:
      # https://github.com/step-security/harden-runner
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: >-
            ${{ env.AUDIT_PR == 'true' && 'audit'
            || vars.STEP_SECURITY_EGRESS_POLICY
            || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Export CI policy
        id: policy
        run: echo "untrusted_pr=${UNTRUSTED_PR}" >> "$GITHUB_OUTPUT"

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Plan
        id: plan
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          BEFORE_SHA: ${{ github.event.before }}
        run: bash scripts/ci/plan.sh

      - name: Plan security audit
        id: security-audit
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          PUSH_AFTER_SHA: ${{ github.event.after }}
        run: bash scripts/ci/security-audit-gate.sh

      - name: Plan wheel publication
        id: wheel-publication
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
        run: bash scripts/ci/plan-wheel-publication.bash

  pre-commit:
    name: >-
      ${{ github.event_name == 'pull_request'
      && github.event.action == 'edited'
      && !github.event.changes.base
      && 'metadata-only pull request edit (lint)'
      || 'pre-commit' }}
    if: >-
      !cancelled() &&
      (github.event_name != 'pull_request' ||
       github.event.action != 'edited' ||
       github.event.changes.base)
    needs: plan
    # Untrusted PRs stay on GitHub-hosted runners
    runs-on: >-
      ${{ github.event_name == 'pull_request'
      && needs.plan.outputs.untrusted_pr != 'false'
      && 'ubuntu-22.04'
      || fromJson('["self-hosted", "Linux", "X64", "build"]') }}
    env:
      # Self-hosted keeps Rust artifacts outside the workspace
      CARGO_TARGET_DIR: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && format('{0}/target', github.workspace)
        || '/home/runner/.cache/cargo-target/pre-commit' }}
      CARGO_CI_PROFILE: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && 'ci-pr'
        || 'nextest' }}
      # Scopes incremental clippy/doc checks; empty for non-PR/push events.
      CHANGED_BASE_SHA: >-
        ${{ github.event.pull_request.base.sha || github.event.before }}
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: >-
            ${{ env.AUDIT_PR == 'true' && 'audit'
            || vars.STEP_SECURITY_EGRESS_POLICY
            || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          # Full history so changed-file scripts can resolve CHANGED_BASE_SHA.
          fetch-depth: 0

      # Temporary: fail fast on rare runner worktree corruption while we isolate the root cause
      - name: Verify immutable CI inputs (post-checkout)
        run: bash scripts/ci/verify-ci-inputs.sh post-checkout

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: "3.13"
          # free-disk-space auto-skips on the nautilus-ci-runner-* pool.
          free-disk-space: "true"
          build-type: "pre-commit"
          rust-cache-enabled: "false"

      - name: Limit Cargo build jobs on GitHub-hosted runners
        if: runner.environment == 'github-hosted'
        run: echo "CARGO_BUILD_JOBS=2" >> "$GITHUB_ENV"

      - name: Get lychee version
        run: |
          version="$(bash scripts/cargo-tool-version.sh lychee)"
          echo "LYCHEE_VERSION=$version" >> "$GITHUB_ENV"

      - name: Install lychee
        # https://github.com/taiki-e/install-action
        uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: lychee@${{ env.LYCHEE_VERSION }}
        env:
          # Ubuntu 22.04 glibc is older than Lychee's GNU binary requires
          CARGO_BUILD_TARGET: x86_64-unknown-linux-musl

      - name: Get cargo-machete version
        run: |
          version="$(bash scripts/cargo-tool-version.sh cargo-machete)"
          echo "CARGO_MACHETE_VERSION=$version" >> "$GITHUB_ENV"

      - name: Install cargo-machete
        # https://github.com/taiki-e/install-action
        uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: cargo-machete@${{ env.CARGO_MACHETE_VERSION }}

      - name: Run pre-commit
        run: prek run --all-files

      - name: Verify capnp schemas are up-to-date
        run: make check-capnp-schemas

  build:
    if: github.ref != 'refs/heads/test-pre-commit' && needs.plan.outputs.run-tests == 'true'
    needs:
      - plan
      - pre-commit
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"
    name: >-
      ${{ github.event_name == 'pull_request'
      && github.event.action == 'edited'
      && !github.event.changes.base
      && 'metadata-only pull request edit (python)'
      || format('build - python {0} (ubuntu-22.04)', matrix.python-version) }}
    runs-on: >-
      ${{ github.event_name == 'pull_request'
      && needs.plan.outputs.untrusted_pr != 'false'
      && 'ubuntu-22.04'
      || fromJson('["self-hosted", "Linux", "X64", "build"]') }}
    defaults:
      run:
        shell: bash
    env:
      CARGO_CI_PROFILE: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && 'ci-pr'
        || 'nextest' }}
      MATURIN_PROFILE: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && 'ci-pr-wheel'
        || 'release' }}
      RUST_BACKTRACE: 1
      # Self-hosted keeps Rust artifacts outside the workspace
      CARGO_TARGET_DIR: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && format('{0}/target', github.workspace)
        || format('/home/runner/.cache/cargo-target/py{0}', matrix.python-version) }}
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: >-
            ${{ env.AUDIT_PR == 'true' && 'audit'
            || vars.STEP_SECURITY_EGRESS_POLICY
            || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      # Temporary: fail fast on rare runner worktree corruption while we isolate the root cause
      - name: Verify immutable CI inputs (post-checkout)
        run: bash scripts/ci/verify-ci-inputs.sh post-checkout

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: ${{ matrix.python-version }}
          # free-disk-space auto-skips on the nautilus-ci-runner-* pool.
          free-disk-space: "true"
          free-disk-space-max-build: ${{ runner.environment == 'github-hosted' && 'true' || 'false' }}
          rust-cache-enabled: "false"

      - name: Limit Cargo build jobs on GitHub-hosted runners
        if: runner.environment == 'github-hosted'
        run: echo "CARGO_BUILD_JOBS=2" >> "$GITHUB_ENV"

      - name: Update version in pyproject.toml
        if: ${{ github.ref != 'refs/heads/master' }}
        working-directory: ${{ env.PACKAGE_DIR }}
        env:
          PUBLISH_WHEEL_VERSION: ${{ needs.plan.outputs.wheel_version }}
        run: |
          bash ../scripts/ci/update-pyproject-version.sh

      # Temporary: fail fast on rare runner worktree corruption while we isolate the root cause
      - name: Verify immutable CI inputs (pre-wheel build)
        run: bash scripts/ci/verify-ci-inputs.sh pre-wheel-build

      - name: Build wheel
        working-directory: ${{ env.PACKAGE_DIR }}
        run: |
          MATURIN_VERSION="$(bash ../scripts/maturin-version.bash)"
          uv run --no-project --no-build --with "maturin==${MATURIN_VERSION}" -- \
            maturin build --profile "$MATURIN_PROFILE" --out ../dist

      - name: Run Python tests
        working-directory: ${{ env.PACKAGE_DIR }}
        run: bash ../scripts/ci/test-wheel.bash

      - name: Check generated file drift
        # Keep this 3.13 condition in sync with the matrix so drift is checked once per run.
        if: matrix.python-version == '3.13'
        env:
          TARGET_DIR: >-
            ${{ github.event_name == 'pull_request'
            && needs.plan.outputs.untrusted_pr != 'false'
            && format('{0}/target/py-stubs', github.workspace)
            || '/home/runner/.cache/cargo-target/py-stubs' }}
        run: |
          make py-stubs
          bash scripts/ci/check-generated-drift.bash

      - name: Upload wheel artifact
        uses: ./.github/actions/upload-artifact-wheel
        with:
          retention-days: ${{ github.ref_name == 'master' && '7' || '1' }}

  rust-tests-linux-x86:
    if: >
      github.ref != 'refs/heads/test-pre-commit' &&
      needs.plan.outputs.run-tests == 'true' &&
      (needs.plan.outputs.run-rust-tests == 'true' ||
       needs.plan.outputs.publish_r2 == 'true' ||
       github.ref == 'refs/heads/master')
    name: rust tests (ubuntu-22.04)
    runs-on: >-
      ${{ github.event_name == 'pull_request'
      && needs.plan.outputs.untrusted_pr != 'false'
      && 'ubuntu-22.04'
      || fromJson('["self-hosted", "Linux", "X64", "build"]') }}
    needs:
      - plan
      - pre-commit
    env:
      CARGO_CI_PROFILE: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && 'ci-pr'
        || github.event_name == 'push'
        && (github.ref_name == 'develop' || github.ref_name == 'test-ci')
        && 'ci-pr'
        || 'nextest' }}
      CARGO_TARGET_DIR: >-
        ${{ github.event_name == 'pull_request'
        && needs.plan.outputs.untrusted_pr != 'false'
        && 'target/rust-tests-linux-x86'
        || github.event_name == 'push'
        && (github.ref_name == 'develop' || github.ref_name == 'test-ci')
        && 'target/rust-tests-linux-x86'
        || '/home/runner/.cache/cargo-target/rust-tests-linux-x86' }}
      RUST_BACKTRACE: 1
    # yamllint disable rule:line-length
    services:
      redis:
        image: public.ecr.aws/docker/library/redis:7.4.5-alpine3.21@sha256:bb186d083732f669da90be8b0f975a37812b15e913465bb14d845db72a4e3e08
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      postgres:
        image: public.ecr.aws/docker/library/postgres:16.4-alpine@sha256:5660c2cbfea50c7a9127d17dc4e48543eedd3d7a41a595a2dfa572471e37e64c
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: pass
          POSTGRES_DB: nautilus
        ports:
          - 5432:5432
        options: --health-cmd "pg_isready -U postgres -d nautilus" --health-interval 10s --health-timeout 5s --health-retries
          5
    # yamllint enable rule:line-length
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: >-
            ${{ env.AUDIT_PR == 'true' && 'audit'
            || vars.STEP_SECURITY_EGRESS_POLICY
            || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Verify immutable CI inputs (post-checkout)
        run: bash scripts/ci/verify-ci-inputs.sh post-checkout

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: "3.13"
          free-disk-space: "true"
          free-disk-space-max-build: ${{ runner.environment == 'github-hosted' && 'true' || 'false' }}
          rust-cache-enabled: >-
            ${{ (runner.environment == 'github-hosted'
            || github.event_name == 'push'
            && (github.ref_name == 'develop' || github.ref_name == 'test-ci'))
            && 'true' || 'false' }}
          rust-cache-key: rust-tests-linux-x86
          rust-cache-workspaces: . -> target/rust-tests-linux-x86
          rust-cache-on-failure: "false"
          rust-cache-save-if: ${{ github.event_name == 'push' }}

      - name: Limit Cargo build jobs on GitHub-hosted runners
        if: runner.environment == 'github-hosted'
        run: echo "CARGO_BUILD_JOBS=2" >> "$GITHUB_ENV"

      - name: Install Nautilus CLI
        env:
          NAUTILUS_CLI_FORCE_SOURCE: "1"
        run: bash scripts/ci/install-nautilus-cli.sh

      - name: Init postgres schema
        run: nautilus database init --schema ${{ github.workspace }}/schema/sql
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USERNAME: postgres
          POSTGRES_PASSWORD: pass
          POSTGRES_DATABASE: nautilus

      - name: Cached test data
        uses: ./.github/actions/common-test-data

      - name: Report disk space before Rust tests
        run: bash scripts/ci/report-disk-space.bash "before Rust tests"

      - name: Run Rust doctests
        run: >-
          make cargo-test-doc
          EXTRA_FEATURES="capnp,hypersync,nautilus-serialization/sbe,nautilus-infrastructure/postgres"

      - name: Report disk space after Rust doctests
        if: always()
        run: bash scripts/ci/report-disk-space.bash "after Rust doctests"

      - name: Run Rust tests
        run: >-
          make cargo-test NEXTEST_PROFILE=ci
          EXTRA_FEATURES="capnp,hypersync,nautilus-serialization/sbe,nautilus-infrastructure/postgres"

      - name: Report disk space after Rust tests
        if: always()
        run: bash scripts/ci/report-disk-space.bash "after Rust tests"

  # Linux ARM64, macOS ARM64, and Windows x86_64 wheels build only on nightly and master
  build-linux-arm:
    if: >
      needs.plan.outputs.run-tests == 'true' &&
      (github.ref_name == 'nightly' || github.ref_name == 'master')
    needs:
      - plan
      - pre-commit
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"
    runs-on: depot-ubuntu-22.04-arm-8
    defaults:
      run:
        shell: bash
    env:
      BUILD_MODE: release
      RUST_BACKTRACE: 1
      CARGO_TARGET_DIR: ${{ github.workspace }}/target/build-linux-arm
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: ${{ matrix.python-version }}
          free-disk-space: "true"
          rust-cache-key: build-linux-arm
          rust-cache-workspaces: . -> target/build-linux-arm
          rust-cache-save-if: ${{ github.event_name == 'push' }}

      - name: Update version in pyproject.toml
        if: ${{ github.ref != 'refs/heads/master' }}
        working-directory: ${{ env.PACKAGE_DIR }}
        env:
          PUBLISH_WHEEL_VERSION: ${{ needs.plan.outputs.wheel_version }}
        run: bash ../scripts/ci/update-pyproject-version.sh

      - name: Build wheel
        working-directory: ${{ env.PACKAGE_DIR }}
        run: |
          MATURIN_VERSION="$(bash ../scripts/maturin-version.bash)"
          uv run --no-project --no-build --with "maturin==${MATURIN_VERSION}" -- \
            maturin build --release --out ../dist

      - name: Run Python tests
        working-directory: ${{ env.PACKAGE_DIR }}
        run: bash ../scripts/ci/test-wheel.bash

      - name: Upload wheel artifact
        uses: ./.github/actions/upload-artifact-wheel
        with:
          retention-days: ${{ github.ref_name == 'master' && '7' || '1' }}

  build-macos:
    if: >
      needs.plan.outputs.run-tests == 'true' &&
      (github.ref_name == 'nightly' || github.ref_name == 'master')
    needs:
      - plan
      - pre-commit
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"
    runs-on: macos-latest
    defaults:
      run:
        shell: bash
    env:
      BUILD_MODE: release
      RUST_BACKTRACE: 1
      CARGO_TARGET_DIR: ${{ github.workspace }}/target/build-macos
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: ${{ matrix.python-version }}
          free-disk-space: "true"
          rust-cache-key: build-macos
          rust-cache-workspaces: . -> target/build-macos
          rust-cache-save-if: ${{ github.event_name == 'push' }}

      - name: Update version in pyproject.toml
        if: ${{ github.ref != 'refs/heads/master' }}
        working-directory: ${{ env.PACKAGE_DIR }}
        env:
          PUBLISH_WHEEL_VERSION: ${{ needs.plan.outputs.wheel_version }}
        run: bash ../scripts/ci/update-pyproject-version.sh

      - name: Build wheel
        working-directory: ${{ env.PACKAGE_DIR }}
        run: |
          MATURIN_VERSION="$(bash ../scripts/maturin-version.bash)"
          uv run --no-project --no-build --with "maturin==${MATURIN_VERSION}" -- \
            maturin build --release --out ../dist

      - name: Run Python tests
        working-directory: ${{ env.PACKAGE_DIR }}
        run: bash ../scripts/ci/test-wheel.bash

      - name: Upload wheel artifact
        uses: ./.github/actions/upload-artifact-wheel
        with:
          retention-days: ${{ github.ref_name == 'master' && '7' || '1' }}

  build-windows:
    if: >
      needs.plan.outputs.run-tests == 'true' &&
      (github.ref_name == 'nightly' || github.ref_name == 'master')
    needs:
      - plan
      - pre-commit
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"
    runs-on: depot-windows-2022-8
    defaults:
      run:
        shell: bash
    env:
      BUILD_MODE: release
      RUST_BACKTRACE: 1
      CARGO_TARGET_DIR: ${{ github.workspace }}/target/build-windows
    steps:
      # Harden-Runner skips its agent on Depot Windows, and its post step assumes
      # C:\agent exists. Keep this job out until upstream fixes cleanup.

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: ${{ matrix.python-version }}
          free-disk-space: "true"
          rust-cache-key: build-windows
          rust-cache-workspaces: . -> target/build-windows
          rust-cache-save-if: ${{ github.event_name == 'push' }}

      - name: Update version in pyproject.toml
        if: ${{ github.ref != 'refs/heads/master' }}
        working-directory: ${{ env.PACKAGE_DIR }}
        env:
          PUBLISH_WHEEL_VERSION: ${{ needs.plan.outputs.wheel_version }}
        run: bash ../scripts/ci/update-pyproject-version.sh

      - name: Build wheel
        working-directory: ${{ env.PACKAGE_DIR }}
        run: |
          MATURIN_VERSION="$(bash ../scripts/maturin-version.bash)"
          uv run --no-project --no-build --with "maturin==${MATURIN_VERSION}" -- \
            maturin build --release --out ../dist

      - name: Run Python tests
        working-directory: ${{ env.PACKAGE_DIR }}
        run: bash ../scripts/ci/test-wheel.bash

      - name: Upload wheel artifact
        uses: ./.github/actions/upload-artifact-wheel
        with:
          retention-days: ${{ github.ref_name == 'master' && '7' || '1' }}

  security-gate-nightly:
    name: security-gate-nightly
    runs-on: ubuntu-latest
    needs:
      - build
      - build-linux-arm
      - build-macos
      - build-windows
    if: github.event_name == 'push' && github.ref == 'refs/heads/nightly'
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.SECURITY_AUDIT_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      # Requires admin access to modify; does not widen attack surface
      - name: Check security gate override
        id: gate
        env:
          SECURITY_GATE_OVERRIDE: ${{ vars.SECURITY_GATE_OVERRIDE }}
        run: |
          override="${SECURITY_GATE_OVERRIDE}"
          if [ -n "$override" ] && [ "$(date -u +%s)" -lt "$(date -u -d "$override" +%s 2>/dev/null || echo 0)" ]; then
            echo "::warning::Security gate override active until $override"
            echo "override_active=true" >> "$GITHUB_OUTPUT"
          else
            echo "override_active=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install cargo-audit
        if: steps.gate.outputs.override_active != 'true'
        uses: ./.github/actions/cargo-tool-install
        with:
          tool-name: cargo-audit

      - name: Run cargo-audit
        if: steps.gate.outputs.override_active != 'true'
        run: |
          cargo audit --deny unsound
          cargo audit --deny unsound --file crates/adapters/lighter/fuzz/pornin/Cargo.lock

      - name: Run osv-scanner
        id: osv-scan
        if: steps.gate.outputs.override_active != 'true'
        continue-on-error: true
        uses: google/osv-scanner-action/osv-scanner-action@9a498708959aeaef5ef730655706c5a1df1edbc2 # v2.3.8
        with:
          scan-args: |-
            --config=osv-scanner.toml
            --format=json
            --output=/github/workspace/osv-results.json
            --lockfile=Cargo.lock
            --lockfile=crates/adapters/lighter/fuzz/pornin/Cargo.lock
            --lockfile=python/uv.lock

      - name: Check OSV severity
        if: steps.osv-scan.outcome == 'failure' && steps.gate.outputs.override_active != 'true'
        run: bash scripts/ci/osv-severity-gate.sh osv-results.json

  publish:
    needs:
      - plan
      - build
      - build-linux-arm
      - build-macos
      - build-windows
      - rust-tests-linux-x86
      - security-gate-nightly
    runs-on: ubuntu-latest
    # Publish dev/nightly wheels to the R2 index. The platform jobs are skipped on develop
    # (Linux x86_64 only there), so guard on results rather than success: require the Linux
    # build and tolerate skipped (but not failed) platform jobs. master uses the dedicated stable
    # release jobs instead, so it is intentionally excluded here.
    if: >
      !cancelled() &&
      needs.build.result == 'success' &&
      needs.build-linux-arm.result != 'failure' &&
      needs.build-macos.result != 'failure' &&
      needs.build-windows.result != 'failure' &&
      needs.rust-tests-linux-x86.result == 'success' &&
      github.event_name == 'push' &&
      needs.plan.outputs.publish_r2 == 'true' &&
      (needs.plan.outputs.wheel_matrix != 'nightly' ||
       needs.security-gate-nightly.result == 'success')
    environment: ${{ needs.plan.outputs.publish_environment }}
    permissions:
      actions: write # Required for deleting artifacts
      contents: read
      id-token: write # Required for attestations
      attestations: write # Required for attestations
    env:
      CLOUDFLARE_R2_REGION: "auto"
      CLOUDFLARE_R2_BUCKET_NAME: "packages"
      CLOUDFLARE_R2_PREFIX: "simple/nautilus-trader"
      PUBLISH_WHEELS_INDEX_URL: "https://packages.nautechsystems.io/simple"
      PUBLISH_WHEEL_MATRIX: ${{ needs.plan.outputs.wheel_matrix }}
      PUBLISH_WHEEL_VERSION: ${{ needs.plan.outputs.wheel_version }}
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            packages.nautechsystems.io:443
            tuf-repo-cdn.sigstore.dev:443
            ${{ secrets.CLOUDFLARE_R2_ALLOWED_HOST }}:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Require successful same-commit security audit
        if: >-
          needs.plan.outputs.publish_development == 'true' &&
          needs.plan.outputs.security-audit-needed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash scripts/ci/check-security-audit-result.sh

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Get uv version
        id: uv-version
        run: echo "uv_version=$(bash scripts/uv-version.sh)" >> "$GITHUB_OUTPUT"

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: ${{ steps.uv-version.outputs.uv_version }}

      - name: Download built wheels
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          pattern: "nautilus_trader-*.whl"
          merge-multiple: true

      # https://github.com/actions/attest-build-provenance
      - name: Attest wheel provenance
        uses: ./.github/actions/attest-build-provenance-retry
        with:
          subject-path: "dist/nautilus_trader-*.whl"

      - name: Verify wheel attestations
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ATTESTATION_IDENTITY: ${{ github.server_url }}/${{ github.workflow_ref }}
          ATTESTATION_ISSUER: https://token.actions.githubusercontent.com
        run: bash ./scripts/ci/verify-gh-attestations.bash dist/nautilus_trader-*.whl

      - name: Publish wheels to Cloudflare R2
        uses: ./.github/actions/publish-wheels
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          CLOUDFLARE_R2_URL: ${{ secrets.CLOUDFLARE_R2_URL }}

      - name: Fetch and delete artifacts for current run
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |-
          bash ./scripts/ci/publish-wheels-delete-artifacts.sh

  cargo-deny:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-22.04
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install cargo-deny
        uses: ./.github/actions/cargo-tool-install
        with:
          tool-name: cargo-deny

      - name: Run cargo-deny (advisories, licenses, sources, bans)
        run: |
          cargo deny --all-features check advisories licenses sources bans
          cargo deny --manifest-path crates/adapters/lighter/fuzz/pornin/Cargo.toml \
            --config .cargo/deny-fuzz.toml --locked --all-features check \
            advisories licenses sources bans

  cargo-vet:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-22.04
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install cargo-vet
        uses: ./.github/actions/cargo-tool-install
        with:
          tool-name: cargo-vet

      - name: Run cargo-vet
        run: |
          cargo vet --locked
          cargo vet --locked --manifest-path crates/adapters/lighter/fuzz/pornin/Cargo.toml \
            --store-path .supply-chain

  release-cargo-publish-preflight:
    name: release cargo publish preflight
    runs-on: ubuntu-latest
    needs:
      - cargo-deny
      - cargo-vet
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            crates.io:443
            index.crates.io:443
            static.crates.io:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: "3.13"
          build-type: "test"
          free-disk-space: "true"
          uv-cache-enabled: "false"
          prek-cache-enabled: "false"
          capnp-cache-enabled: "false"
          rust-cache-enabled: "false"
          rust-cache-save-if: "false"

      - name: Check Cargo crate publish plan
        run: bash scripts/ci/publish-cargo-crates.sh --check

      - name: Dry-run Cargo crate publishing
        run: bash scripts/ci/publish-cargo-crates.sh --dry-run

  release-docs-features-preflight:
    name: release docs/features preflight
    runs-on: [self-hosted, Linux, X64, self-hosted-linux-x86]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    permissions:
      contents: read
    env:
      CARGO_TARGET_DIR: /home/runner/.cache/cargo-target/release-docs-features-preflight
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: "3.13"
          free-disk-space: "true"
          rust-cache-enabled: "false"

      - name: Install Rust nightly
        run: rustup toolchain install nightly

      # https://github.com/taiki-e/install-action
      - name: Install cargo-hack
        uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: cargo-hack

      - name: Run docsrs-check
        run: make docsrs-check

      - name: Run check-features
        run: make check-features

      - name: Run check-all-targets
        run: make check-all-targets

  tag-release:
    needs:
      - build
      - build-linux-arm
      - build-macos
      - build-windows
      - cargo-deny
      - cargo-vet
      - release-cargo-publish-preflight
      - release-docs-features-preflight
      - rust-tests-linux-x86
    permissions:
      contents: write # Required for pushing tags and creating the draft release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    outputs:
      package_version: ${{ steps.metadata.outputs.package_version }}
      tag_name: ${{ steps.metadata.outputs.tag_name }}
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: true # Required for salsify action to push git tags
          fetch-depth: 2
          fetch-tags: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Set release metadata
        id: metadata
        shell: bash
        run: |
          version="$(./scripts/package-version.sh)"
          release_kind="$(bash scripts/ci/release-version-policy.bash "$version")"
          if [[ "$(sed -n '1p' RELEASES.md)" != "# NautilusTrader ${version}" ]]; then
            echo "::error::RELEASES.md does not start with NautilusTrader ${version}."
            exit 1
          fi
          prerelease=false
          if [[ "$release_kind" == "prerelease" ]]; then
            prerelease=true
          fi
          {
            echo "PACKAGE_VERSION=${version}"
            echo "RELEASE_NAME=NautilusTrader ${version}"
            echo "RELEASE_PRERELEASE=${prerelease}"
            echo "TAG_NAME=v${version}"
          } >> "$GITHUB_ENV"
          {
            echo "package_version=${version}"
            echo "tag_name=v${version}"
          } >> "$GITHUB_OUTPUT"
          sed -n '/^#/,$ {p;/^---/q}; w RELEASE.md' RELEASES.md

      - name: Create git tag
        # https://github.com/salsify/action-detect-and-tag-new-version
        uses: salsify/action-detect-and-tag-new-version@b1778166f13188a9d478e2d1198f993011ba9864 # v2.0.3
        with:
          version-command: ./scripts/package-version.sh

      - name: Verify release tag
        shell: bash
        run: |
          tag_commit="$(git rev-parse "${TAG_NAME}^{commit}" 2>/dev/null || true)"
          if [[ "$tag_commit" != "$GITHUB_SHA" ]]; then
            echo "::error::${TAG_NAME} targets ${tag_commit:-<missing>}, expected ${GITHUB_SHA}."
            exit 1
          fi

      - name: Create draft GitHub release
        id: create-release
        # https://github.com/softprops/action-gh-release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: ${{ env.RELEASE_NAME }}
          draft: true
          prerelease: ${{ env.RELEASE_PRERELEASE }}
          body_path: RELEASE.md

  build-sdist:
    needs:
      - tag-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            pypi.org:443
            files.pythonhosted.org:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: "3.13"
          free-disk-space: "true"
          uv-cache-enabled: "false"
          prek-cache-enabled: "false"
          capnp-cache-enabled: "false"
          rust-cache-enabled: "false"

      - name: Build sdist
        run: bash ./scripts/ci/build-sdist-release-asset.bash

      - name: Upload sdist artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-sdist
          path: dist/nautilus_trader-*.tar.gz
          if-no-files-found: error
          retention-days: 7

  upload-sdist-release:
    needs:
      - build-sdist
      - tag-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      actions: read # Required for downloading the sdist artifact
      contents: write # Required for uploading release assets
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Download sdist artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-sdist
          path: release-sdist

      - name: Upload sdist to GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
        run: bash ./scripts/ci/upload-github-release-assets.bash sdist release-sdist/nautilus_trader-*.tar.gz

  publish-wheels-master:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      actions: read # Required for downloading build artifacts
      contents: write # Required for uploading release assets
    needs:
      - build
      - build-linux-arm
      - build-macos
      - build-windows
      - tag-release
      - upload-sdist-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    env:
      CLOUDFLARE_R2_REGION: "auto"
      CLOUDFLARE_R2_BUCKET_NAME: "packages"
      CLOUDFLARE_R2_PREFIX: "simple/nautilus-trader"
      PUBLISH_WHEELS_INDEX_URL: "https://packages.nautechsystems.io/simple"
      PUBLISH_WHEEL_MATRIX: stable
      PUBLISH_WHEEL_VERSION: ${{ needs.tag-release.outputs.package_version }}
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            packages.nautechsystems.io:443
            ${{ secrets.CLOUDFLARE_R2_ALLOWED_HOST }}:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Get uv version
        id: uv-version
        run: echo "uv_version=$(bash scripts/uv-version.sh)" >> "$GITHUB_OUTPUT"

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: ${{ steps.uv-version.outputs.uv_version }}

      - name: Download built wheels
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          pattern: "nautilus_trader-*.whl"
          merge-multiple: true

      - name: Upload wheels to GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
        run: bash ./scripts/ci/upload-github-release-assets.bash wheels dist/nautilus_trader-*.whl

      - name: Publish wheels to Cloudflare R2
        uses: ./.github/actions/publish-wheels
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          CLOUDFLARE_R2_URL: ${{ secrets.CLOUDFLARE_R2_URL }}

  publish-wheels-pypi:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      actions: read # Required for downloading build artifacts
      contents: read
      id-token: write # Required for PyPI Trusted Publishing and publish attestations
      attestations: write # Required for GitHub artifact attestations
    needs:
      - publish-wheels-master
      - tag-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            tuf-repo-cdn.sigstore.dev:443
            rekor.sigstore.dev:443
            pypi.org:443
            files.pythonhosted.org:443
            upload.pypi.org:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Get release Python tool versions
        shell: bash
        run: |
          echo "UV_VERSION=$(bash scripts/uv-version.sh)" >> "$GITHUB_ENV"
          echo "PYPI_ATTESTATIONS_VERSION=$(bash scripts/tool-version.sh pypi-attestations)" >> "$GITHUB_ENV"

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: false

      - name: Check Rekor readiness
        run: bash ./scripts/ci/check-rekor-ready.bash

      - name: Download built wheel artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          pattern: "nautilus_trader-*.whl"
          merge-multiple: true

      - name: Generate PyPI publish attestations
        run: bash ./scripts/ci/sign-pypi-attestations.bash dist/nautilus_trader-*.whl

      # https://github.com/actions/attest-build-provenance
      - name: Attest wheel provenance
        id: attest-wheels
        uses: ./.github/actions/attest-build-provenance-retry
        with:
          subject-path: "dist/nautilus_trader-*.whl"

      - name: Verify wheel attestations
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ATTESTATION_IDENTITY: ${{ github.server_url }}/${{ github.workflow_ref }}
          ATTESTATION_ISSUER: https://token.actions.githubusercontent.com
        run: bash ./scripts/ci/verify-gh-attestations.bash dist/nautilus_trader-*.whl

      - name: Publish wheels to PyPI
        if: success()
        run: bash ./scripts/ci/publish-pypi-trusted.bash wheels

      - name: Prepare wheel attestation siblings
        shell: bash
        env:
          BUNDLE_PATH: ${{ steps.attest-wheels.outputs.bundle-path }}
        run: bash ./scripts/ci/prepare-release-attestation-siblings.bash dist/nautilus_trader-*.whl

      - name: Upload wheel attestation siblings
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-wheel-attestation-siblings
          path: |
            dist/nautilus_trader-*.whl.sigstore
            dist/nautilus_trader-*.whl.intoto.jsonl
          if-no-files-found: error
          retention-days: 7

  publish-cargo-crates:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: read
      id-token: write # Required for crates.io Trusted Publishing
    needs:
      - publish-wheels-master
      - tag-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            crates.io:443
            index.crates.io:443
            static.crates.io:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Common setup
        uses: ./.github/actions/common-setup
        with:
          python-version: "3.13"
          build-type: "release"
          uv-cache-enabled: "false"
          prek-cache-enabled: "false"
          capnp-cache-enabled: "false"
          rust-cache-enabled: "false"
          rust-cache-save-if: "false"

      - name: Check Cargo crate publish plan
        run: bash scripts/ci/publish-cargo-crates.sh --check

      - name: Authenticate to crates.io
        id: crates-io-auth
        # https://github.com/rust-lang/crates-io-auth-action
        uses: rust-lang/crates-io-auth-action@bbd81622f20ce9e2dd9622e3218b975523e45bbe # v1.0.4

      - name: Publish Cargo crates
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-io-auth.outputs.token }}
          CARGO_PUBLISH_ATTEMPTS: "5"
          CARGO_PUBLISH_POLL_SECONDS: "5"
          CARGO_PUBLISH_RETRY_DELAY_SECONDS: "15"
          CARGO_PUBLISH_SUCCESS_DELAY_SECONDS: "0"
          CARGO_PUBLISH_WAIT_TIMEOUT_SECONDS: "120"
          CARGO_PUBLISH_USER_AGENT: >-
            nautilus-trader-release
            (${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
        run: bash scripts/ci/publish-cargo-crates.sh

  publish-sdist-pypi:
    needs:
      - publish-wheels-pypi
      - tag-release
      - upload-sdist-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment: release
    permissions:
      actions: read # Required for downloading the sdist artifact
      contents: read
      id-token: write # Required for PyPI Trusted Publishing and publish attestations
      attestations: write # Required for GitHub artifact attestations
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            tuf-repo-cdn.sigstore.dev:443
            rekor.sigstore.dev:443
            pypi.org:443
            files.pythonhosted.org:443
            upload.pypi.org:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Get release Python tool versions
        shell: bash
        run: |
          echo "UV_VERSION=$(bash scripts/uv-version.sh)" >> "$GITHUB_ENV"
          echo "PYPI_ATTESTATIONS_VERSION=$(bash scripts/tool-version.sh pypi-attestations)" >> "$GITHUB_ENV"

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: false

      - name: Check Rekor readiness
        run: bash ./scripts/ci/check-rekor-ready.bash

      - name: Download sdist artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-sdist
          path: dist

      - name: Generate PyPI publish attestation
        run: bash ./scripts/ci/sign-pypi-attestations.bash dist/nautilus_trader-*.tar.gz

      # https://github.com/actions/attest-build-provenance
      - name: Attest sdist provenance
        id: attest-sdist
        uses: ./.github/actions/attest-build-provenance-retry
        with:
          subject-path: "dist/nautilus_trader-*.tar.gz"

      - name: Verify sdist attestation
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ATTESTATION_IDENTITY: ${{ github.server_url }}/${{ github.workflow_ref }}
          ATTESTATION_ISSUER: https://token.actions.githubusercontent.com
        run: bash ./scripts/ci/verify-gh-attestations.bash dist/nautilus_trader-*.tar.gz

      - name: Publish sdist to PyPI
        if: success()
        run: bash ./scripts/ci/publish-pypi-trusted.bash sdist

      - name: Prepare sdist attestation sibling
        shell: bash
        env:
          BUNDLE_PATH: ${{ steps.attest-sdist.outputs.bundle-path }}
        run: bash ./scripts/ci/prepare-release-attestation-siblings.bash dist/nautilus_trader-*.tar.gz

      - name: Upload sdist attestation sibling
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-sdist-attestation-sibling
          path: |
            dist/nautilus_trader-*.tar.gz.sigstore
            dist/nautilus_trader-*.tar.gz.intoto.jsonl
          if-no-files-found: error
          retention-days: 7

  publish-release-integrity:
    needs:
      - publish-cargo-crates
      - publish-wheels-pypi
      - publish-sdist-pypi
      - tag-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      actions: write # Required for downloading and deleting release artifacts
      contents: write # Required for uploading release integrity assets
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            tuf-repo-cdn.sigstore.dev:443
            rekor.sigstore.dev:443
            pypi.org:443
            files.pythonhosted.org:443
            crates.io:443
            index.crates.io:443
            static.crates.io:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Get release Python tool versions
        shell: bash
        run: |
          echo "UV_VERSION=$(bash scripts/uv-version.sh)" >> "$GITHUB_ENV"
          echo "PYPI_ATTESTATIONS_VERSION=$(bash scripts/tool-version.sh pypi-attestations)" >> "$GITHUB_ENV"

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: false

      - name: Check Rekor readiness
        run: bash ./scripts/ci/check-rekor-ready.bash

      - name: Generate release checksums
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
          ATTESTATION_IDENTITY: ${{ github.server_url }}/${{ github.workflow_ref }}
          ATTESTATION_ISSUER: https://token.actions.githubusercontent.com
        run: bash ./scripts/ci/publish-release-checksums.sh --generate-only release-assets

      - name: Verify published registries
        shell: bash
        env:
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYPI_PUBLISHER_WORKFLOW: build.yml
          PYPI_PUBLISHER_ENVIRONMENT: release
          CRATES_IO_MANUAL_PUBLISH_EXCEPTIONS: ${{ vars.CRATES_IO_MANUAL_PUBLISH_EXCEPTIONS }}
          CARGO_PUBLISH_USER_AGENT: >-
            nautilus-trader-release-verifier
            (${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
        run: bash ./scripts/ci/verify-published-registries.bash release-assets

      - name: Publish release checksums
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
          ATTESTATION_IDENTITY: ${{ github.server_url }}/${{ github.workflow_ref }}
          ATTESTATION_ISSUER: https://token.actions.githubusercontent.com
        run: bash ./scripts/ci/publish-release-checksums.sh --publish-existing release-assets

      - name: Upload crates manifest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
        run: >-
          bash ./scripts/ci/upload-github-release-assets.bash
          crates-manifest
          release-assets/crates-manifest.json

      - name: Download attestation sibling artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: release-attestations
          pattern: "release-*-attestation-*"
          merge-multiple: true

      - name: Attach attestation siblings to GitHub release
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
        run: bash ./scripts/ci/upload-release-attestation-siblings.bash release-attestations

      # A failed integrity job keeps workflow artifacts available for re-runs
      - name: Delete artifacts for current run
        if: success()
        continue-on-error: true
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash ./scripts/ci/publish-wheels-delete-artifacts.sh

  publish-github-release:
    needs:
      - publish-release-integrity
      - tag-release
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      attestations: read # Required for verifying the immutable release attestation
      contents: write # Required for publishing the draft release
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: ${{ vars.STEP_SECURITY_EGRESS_POLICY || 'block' }}
          allowed-endpoints: >-
            ${{ vars.COMMON_ALLOWED_ENDPOINTS }}
            ${{ vars.CI_ALLOWED_ENDPOINTS }}
            rekor.sigstore.dev:443

      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Verify final release assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
          GH_RELEASE_EXPECT_DRAFT: "true"
        run: bash ./scripts/ci/verify-github-release-assets.bash

      - name: Check Rekor readiness
        run: bash ./scripts/ci/check-rekor-ready.bash

      - name: Publish GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
        run: bash ./scripts/ci/publish-github-release.bash

      - name: Verify GitHub release attestation
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.tag-release.outputs.tag_name }}
        run: bash ./scripts/ci/verify-github-release-attestation.bash
